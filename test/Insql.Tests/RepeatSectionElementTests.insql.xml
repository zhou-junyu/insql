<?xml version="1.0" encoding="utf-8" ?>
<insql type="Insql.Tests.RepeatSectionElementTests,Insql.Tests" >

  <select id="RepeatIn">
    select * from user_info where user_id in <repeat name="userIdList" open="(" separator="," close=")" prefix="@"  />
  </select>

  <select id="RepeatInNull">
    select * from user_info
    <where>
      <if test="userIdList !=null and userIdList.length >0">
        user_id in <repeat name="userIdList" open="(" separator="," close=")" prefix="@"  />
      </if>
    </where>
  </select>
</insql>